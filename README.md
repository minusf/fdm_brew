# brew formula for fdm

- `brew create --no-fetch tdb`
- replace contents with `tdb.rb`
- `brew install --build-from-source ./tdb.rb`

- `brew create --no-fetch fdm`
- replace contents with `fdm.rb`
- `brew install --build-from-source ./fdm.rb`
